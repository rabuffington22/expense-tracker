# Hosted Browser CI Observation Trigger — Work Block 4BM

Date: 2026-07-26

This sanitized marker exists only to trigger one confirmed pull-request observation of the durable `Synthetic CI` core-then-browser workflow.

It contains no product change, workflow change, dependency change, credential, protected data, real financial row, deployment instruction, or merge authorization.

The draft PR must remain unmerged. Close it only after both jobs succeed cleanly, their order, steps, runtime, cleanup, annotations, and installed-Chrome behavior are verified, and no Fly deployment exists for the feature SHA. A failed, missing, over-time, out-of-order, or unexpectedly annotated run remains open for read-only diagnosis without repair, rerun, cancellation, or scope expansion.
